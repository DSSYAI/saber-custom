# Saber 精简自定义版 v1.36.1-custom.1

基于官方 Saber **v1.36.1** 加全部 14 个补丁构建的安卓包（仅 arm64-v8a）。

## 附件

- `Saber-arm64-v1.36.1.apk` —— 46.33 MB
- SHA-256：`38d080a6d26fa92f82f67d2de1b118ef5eb41d79c1d837a78f323ec55bf21dd4`
- 构建自本地提交 `fed83adb`，也就是「上游 v1.36.1 + `patches/` 里全部 14 个补丁」的状态（每个补丁的文件名里带它自己的提交号）

## 装之前必读

1. 本包用项目自带的 `android/fallback-key.jks` 签名，**与官方版不同** → 必须先卸载官方 Saber，否则会提示"应用未安装 / 签名冲突"。
2. 卸载会清空应用私有目录（上游清单里写了 `allowBackup="false"`）。**已有笔记的，先用"设置 → 自定义数据目录"挪到共享文件夹或导出备份，再卸载。**
3. 这个包只做过 `flutter analyze` ＋ 测试套件 ＋ 构建产物静态校验（只含 arm64-v8a、apksigner 通过），**没在真机上跑过**——请自行验一遍。

## 包含的改动

- **删除**：Sentry 遥测与崩溃上报、更新检查、赞助/隐私外链、Nextcloud 云同步、官方账号登录入口
- **新增**：系统相册多选导入图片、支持「用 Saber 打开」外部文件、PDF 按页范围导出
- **图片编辑**：非破坏式裁剪、复制一份、置顶/置底、真·反色、水平翻转、自由旋转（箭头跟手、吸附、不裁边）
- **修掉**：启动即闪退（manifest 里遗留的 provider 声明）、把手"看得见却点不到"、旋转时图片变小/被裁边

每处改动的根因、被证伪的猜测与验证方式，都写在 `patches/` 里对应补丁的提交信息中。